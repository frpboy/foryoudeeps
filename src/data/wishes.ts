import type { Wish } from '@/types';

// Add contributor-approved wishes and media here. The renderer supports every
// typed wish variant without component changes.
export const wishes: Wish[] = [];
